cmake_minimum_required(VERSION 3.5)

# Set extension name here
set(TARGET_NAME cnshift)

set(EXTENSION_NAME ${TARGET_NAME}_extension)
set(LOADABLE_EXTENSION_NAME ${TARGET_NAME}_loadable_extension)

project(${TARGET_NAME})

set(CMAKE_CXX_STANDARD "17" CACHE STRING "C++ standard to enforce")
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Formal C++ workspace is ext/src (algorithm + geometry).
# DuckDB codegen requires src/include/{name}_extension.hpp at repo root.
include_directories(src/include ext/src/include)

set(EXTENSION_SOURCES
    ext/src/cnshift_extension.cpp
    ext/src/cnshift_transform.cpp
    ext/src/cnshift_geometry.cpp)

build_static_extension(${TARGET_NAME} ${EXTENSION_SOURCES})
build_loadable_extension(${TARGET_NAME} " " ${EXTENSION_SOURCES})

install(
  TARGETS ${EXTENSION_NAME}
  EXPORT "${DUCKDB_EXPORT_SET}"
  LIBRARY DESTINATION "${INSTALL_LIB_DIR}"
  ARCHIVE DESTINATION "${INSTALL_LIB_DIR}")
